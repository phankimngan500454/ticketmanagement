class: TicketEvent
table: ticket_events
fields:
  ticketId: int, relation(parent=tickets)
  userId: int, relation(parent=app_users)
  eventType: String
  oldValue: String?
  newValue: String?
  description: String?
  createdAt: DateTime
