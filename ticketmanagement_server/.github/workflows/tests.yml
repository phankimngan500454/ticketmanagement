name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.4.4'
      SERVERPOD_PASSWORD_database: 'wOTP-LHz02HIPnwTkXm6XTSMD2Ym-sVw'
      SERVERPOD_PASSWORD_redis: 'Uh7cxf7h-zxbNUVOmWs-mb1XS8n5utDe'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: ticketmanagement_server_server
        run: docker compose up --quiet-pull --quiet-build --build -d

      - name: Install dependencies
        working-directory: ticketmanagement_server_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: ticketmanagement_server_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: ticketmanagement_server_server
        run: serverpod generate

      - name: Run tests
        working-directory: ticketmanagement_server_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: ticketmanagement_server_server
        run: docker compose down -v
